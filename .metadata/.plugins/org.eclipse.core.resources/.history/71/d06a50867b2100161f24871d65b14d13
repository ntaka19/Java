//a container containing a tea
class TeaContainer extends AContainer{

  TeaContainer(Tea content) {
    super(content);
  }
  //to take the content out from this box
  /*Tea get() {
    return content;
  }
  //to swap the content in this container with the one in that container
  void swap(TeaContainer that) {
    Tea c = this.content;
    this.content = that.content;
    that.content = c;
    */
  }
}
